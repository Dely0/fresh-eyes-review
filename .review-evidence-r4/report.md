### 对抗性复审 · 目标：.review-evidence-r3/report.md（9 条）

## 我确认成立的
- F1 上一轮的发现属实且已修好：必需小节从「命中任一个即通过」改成逐个判定 —— 只带 `## 已定取舍`、没有 `## 范围` 的交接文档现在退 1（改动前退 0） · 严重度：中
- F2 上一轮的发现属实且已修好：`provenance_of` 现在要求出处紧跟在「依据：」之后 —— 条目写「依据：未经 HumanDecision 确认」现在退 1（改动前退 0） · 严重度：中
- F6 上一轮的发现属实且已修好：`--falsify` 与 `--replay` / `--pre-fix` 同给现在退 2，与选项顺序无关（`--replay --falsify` 也退 2），不再是「逐字节相同的假通过」 · 严重度：中
- F7 上一轮的发现属实且已修好：README 退出码 `1` 行已补「交接文档的取舍缺出处默认只提醒，`--strict` 下才计不合格」，SKILL.md 的「两个小节是否各自都在」现在与实现一致；交接文档对 F7 的「部分驳回」与上一轮报告自己的认定（SKILL 那句属实）不矛盾 · 判定：成立的那半已修，驳回的那半站得住
- F9 上一轮的发现属实且已修好：inline code 加后置注解的证据路径与反向前置证据路径现在都能解析 —— 同一份报告改动前因「证据文件读不了」退 1，现在 `--replay --strict` 退 0 · 严重度：中
- F4 有一半确实修好了：`falsify_needs_handoff` 夹具补上 `report.md` 后，「`--falsify` 必须配 `--handoff`」这条规则被钉住了 —— 删掉它套件变红（M1「期望 exit=2，实得 exit=0」），只改那句 stderr 文案也变红（M10），不再是「报告读不了」那条分叉在替它变绿 · 判定：该条规则确已钉住
- 取舍#1（归一化只折叠十类已知易变 token）的边界成立：按交接文档自己给的「请只报」口径（只报「静默把真实伪造判成通过」）在本机复跑重放族用例，没有找到边界之外的静默放行；未识别的易变内容只会造成假不一致，那正是有意选的保守一侧（承接 F4 那一组重放规则） · 严重度：低

## 我驳倒的（上一轮报错了，或修复没修好）
- F8 上一轮说「交接文档自报 53 条断言，实际 36 条」，实际是：53→36 这半改对了，但换上了另一个错数 —— 交接文档第 16 行写「新增后合计 79 条全绿」，套件实测「共 83 条断言，变红 1 条」，且旧 34 个用例名一个不少地全在新套件里（34 + 47 新用例 + 2 条手写 = 83）；同一份文档第 24 行仍写「期望 exit 0，输出『共 70 条断言，变红 0 条』」；README:85 写 42 条、README:325 与 SKILL.md:222 写 70 条。它的「范围」一节里的 diff 数字也全错：自报 +1127/-57、门禁 +536、测试 +277，`git diff --numstat` 实为 +1339/-62、+625、+375 · 判定：修复未生效（只改了 53→36） · 严重度：高
- F4 上一轮说「回归套件完全钉不住 `--falsify`」，实际是「三条规则只钉住了一条」：定向变异下 M2（删掉对抗性复审「缺必需小节」规则）与 M3（删掉「判定必须带标注」规则）套件仍然「83 条全绿、0 红」；其中 `falsify_unmarked_claim` 是伪绿断言 —— 它的夹具只有「我确认成立的」一节，判红靠「缺少小节」，把标注规则整条删掉后它对同一夹具的退出码与输出逐字节不变（三段齐全、缺标注的夹具在 M3 下退 0）。所以交接文档「F4 已按它的建议修夹具并补逐条断言，并自跑定向变异复核」不成立 · 判定：修复未生效（部分） · 严重度：高
- F4 的同一处修复还引入了新红灯：新增断言 `falsify_requires_handoff_flag` 比对 stderr 里的中文子串，而门禁只对 `sys.stdout` 调了 `reconfigure(encoding='utf-8')`、stderr 仍是本机 cp936 —— 按交接文档给的原样命令 `python fresh-eyes-review/scripts/test_check_review_report.py` 跑，实得「共 83 条断言，变红 1 条」、exit 1（加 `PYTHONUTF8=1` 才变 0 红）；同一条编码不对称也让门禁所有中文 stderr 消息（含 F6 修复新增的那句）重定向到日志时变乱码 · 判定：修复引入新红灯 · 严重度：高
- F5 上一轮说「对抗性复审可以完全空洞」，实际是「更省事的空洞形态仍然通过」：小节与条目的扫描不跳围栏代码块 —— 一份正文只有 ``` 围栏里的「格式示例」（里面写着 `## 我确认成立的` / `## 我驳倒的` / `## 未决` 和带 F1/F2/F3、带判定的条目）的报告，`--falsify --strict` 退 0 并打印「对抗性复审通过 —— 分段齐全，每条判定都标了出处。」；同一句话在默认档还会与「判定条目没有标注」的 WARN 一起打印，正是上一轮指出的那句不成立的话术 · 判定：修复不完整 · 严重度：中
- F5 的修复还引入假失败：`FINDING_REF_RE = \bF\d+\b` 不认中文相邻写法 —— `- F1这个问题确实存在 · 严重度：高`、`- F2这条上轮报错了 · 严重度：低`、`- F9涉及 CI 行为` 三段全被判「没有点名任何一条上一轮发现」退 1；这是把合法报告按硬失败拦下（属于取舍#4「`--strict` 会误伤合法报告」声明边界之外的一类，且它连默认档都拦） · 判定：修复引入假失败 · 严重度：中
- F9 的修复是过度纠正：`extract_path` 只要值里出现反引号就取第一段反引号内容当路径 —— `- 证据文件：ev/F1.txt（同 \`probe.log\` 那份）`（`ev/F1.txt` 真实存在）被读成 `probe.log`，报「证据文件读不了」退 1；上一轮的误杀换了个方向复现 · 判定：修复引入新问题 · 严重度：中
- F7/F8 名下新增的「同上」支持只接了一半（作者未提）：`check_red` 现在把「反向前置：同上」解析成复现命令，但两条真正会执行命令的路径用的仍是 `is_blank(fields["red"])`，而 `is_blank` 把「同上」当空值 —— `--pre-fix` 直接判 NO-EVIDENCE 退 1，提示「没有『反向前置』，无法核实缺陷是否真的被观察到失败」，可那一行明明就写在报告里；`--replay` 则整条跳过「先红后绿」的红半边，照样打印「格式达标且全部重放一致 —— 每条都真的跑过」（默认档退 0，只多一条提醒）。同一份报告默认档 `--strict` 退 0、`--pre-fix` 退 1，而唯一的断言 `red_same_as_repro` 只钉了默认档那一条路径，`--pre-fix` / `--replay` 两条路径没有任何断言 · 判定：修复不完整（半接线），且 `--pre-fix` 是 README 写明「不是可选项」的那一档 · 严重度：中
- F3 交接文档的驳回理由不成立：上一轮报的是「三种出处标记在门禁下完全等价」（纯机械命题），交接文档自己也承认「标记等价属实」，却用「真人到底有没有拍板不可机械核验」来答复 —— 那是另一个命题。修复后重新实测：同一条取舍写 `OrchestratorClaim` 退 0、零提醒，改写 `HumanDecision` 同样退 0、零提醒。要让诚实标注不吃亏，只需让 `OrchestratorClaim` 走提醒通道，这一步是纯机械的，不必去验真人 · 判定：驳回理由站不住，机械事实仍复现 · 严重度：中
- F1 的修复还留了围栏缺口：小节判定不跳围栏代码块 —— `## 范围` 只出现在 ``` 示例里的交接文档不再报「缺少必需小节」，整份文档退 0（与上面 F5 那条同一根因）。交接文档模板自己就带一个含 `## 范围` 的围栏示例，抄模板时很容易撞上 · 判定：修复不完整 · 严重度：低
- F7/F8 被新用例注释引用错了：`red_same_as_repro` 的注释说「第 3 轮报告的 F7/F8 就是这样被误杀的」，实测不成立 —— 用改动前的门禁跑第 3 轮报告全文（`--strict --workdir .`）是「共 9 条发现：9 条合格，0 条不合格，另有 0 条提醒」、退 0，全文「太短」出现 0 次；第 3 轮 F7/F8 的反向前置都是很长的「同上（…）」，旧门禁的 8 字符阈值本来就不会判它太短。（「同上」支持本身另有正当动机：裸写「同上」在旧门禁下确实会被 `--strict` 判红，只是举的例子不是这两条） · 严重度：低
- F9 附带（取舍核对）：`.gitignore` 只有 `.review-evidence/` 这一条精确规则，`.review-evidence-r3/`、`.review-evidence-r4/`、`.review-handoff.md` 的 `git check-ignore` 全部退 1（未被忽略，`git status` 里就是未跟踪项），而本轮交接文档第 21 行自称「该目录已被 .gitignore 忽略」，新的对抗性复审提示词又让审查者改用 `.review-evidence-round2/` 这类每轮新目录 · 判定：交接文档这条声明不实 · 严重度：低

## 未决
- F3 的另一半（`HumanDecision` 背后是否真有真人拍板）我在本机没法判定：仓库只有一个基线提交，全仓库检索这三个标记只命中门禁、模板、测试夹具与这份交接文档，没有任何会话/工单/issue 记录；我试过 `git log --oneline`、`git show HEAD`、对仓库内 `HumanDecision`/`RecordedDecision` 的全量 grep。
- F1/F5 的围栏缺口我只构造了「格式示例放在 ``` 里」这一种形态，没有穷举所有「能骗过小节/条目扫描、人眼却看不到判决」的写法（注释、表格、引用块），所以这类绕过是否还有别的形态未定。
- 取舍#1 我只按交接文档给的「请只报静默把伪造判成通过」口径找了一轮，没有为 10 条 normalizer 逐条构造对照证据；「折叠掉有意义内容」这条更细的边界是否也成立，本轮没有判定。
